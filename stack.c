#include "monty.h"
#include <stdio.h>
#include <stdlib.h>

/**
 * stack - Sets format stack (LIFO).
 * @stack: *ptr to top of stack.
 * @line_number: current line number in the Monty bytecode
 */

void stack(stack_t **stack, unsigned int line_number)
{
	(void)stack;
	(void)line_number;
}
