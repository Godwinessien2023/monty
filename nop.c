#include "monty.h"
#include <stdlib.h>
#include <stdio.h>

/**
 * nop - does nothing.
 * @top: *ptr that points to the head *ptr
 * @line_number: line number where the opcode appears.
 * Return: void
 */

void nop(stack_t **top, unsigned int line_number)
{
	(void)top;
	(void)line_number;
}
